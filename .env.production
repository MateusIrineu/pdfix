# Configuração para deploy na Azure App Service
# IMPORTANTE: Este arquivo NÃO deve ser commitado se contiver valores reais!
# Para produção, configure as variáveis no Azure App Service → Configuration → Application Settings

NODE_ENV=production
PORT=8080
NEXT_TELEMETRY_DISABLED=1

